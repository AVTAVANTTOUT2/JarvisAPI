# Approbation des scripts postinstall (esbuild telecharge son binaire natif).
# onlyBuiltDependencies : pnpm <= 10 ; allowBuilds : pnpm >= 11.
packages:
  - .

onlyBuiltDependencies:
  - esbuild
allowBuilds:
  esbuild: true
auditConfig:
  ignoreGhsas:
    - GHSA-qwww-vcr4-c8h2
