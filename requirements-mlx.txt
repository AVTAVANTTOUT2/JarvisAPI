# Sidecars audio MLX, installés dans JARVIS_VENV (venv séparé du backend).
# mlx-audio apporte le runtime importé par native_audio/qwen3_local.py, seul
# moteur vocal du projet.
# La borne haute est volontaire : le module `qwen3_tts` et son tokenizer de
# parole sont apparus en 0.4 ; une montée de version doit être mesurée avant
# d'être adoptée (voir scripts/benchmark_tts.py). Validé sur 0.4.5.
mlx-audio>=0.4,<0.5
soundfile>=0.13   # lecture de l'échantillon de voix de référence
