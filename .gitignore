# ── Python ──────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# ── Environnement ───────────────────────────────────────────
.env
.env.config
.env.local
.env.*.local
.env.bak

# ── Données locales ─────────────────────────────────────────
/data/
data/private/
data/voice-tests/
uploads/

# ── Voix (échantillons personnels, jamais versionnés) ───────
voices/**/*.wav
voices/**/*.mp3
voices/**/*.m4a
voices/**/*.flac
voices/**/*.npy
voices/**/*.npz
voices/**/*.pt
voices/**/*.pth
voices/**/*.safetensors
voices/**/*tokens*
voices/**/transcript.txt
data/voices/**/*.wav
data/voices/**/*.mp3
data/voices/**/*.flac
data/voices/**/*.npy
data/voices/**/*.npz
data/voices/**/*.pt
data/voices/**/*.pth
data/voices/**/*.safetensors
data/voices/**/*tokens*

# ── Certificats SSL (ne JAMAIS committer les clés privées) ──
certs/*.pem
certs/*.key
certs/*.crt
certs/*.p12
# Le .gitkeep est volontairement exclu de cette règle

# ── Frontend ────────────────────────────────────────────────
web/node_modules/
web/dist/
web/.vite/
.pnpm-store/

# ── Android ─────────────────────────────────────────────────
android/.gradle/
android/.kotlin/
android/local.properties
android/app/build/
android/app/google-services.json
android/release.keystore
android/signing.properties
credentials/firebase-*.json
jarvis-companion.apk
*.aab

# ── Modèles ML (gros fichiers locaux) ─────────────────────
models/
models/fish*/
# ── Modèles audio locaux (hors Git) ─────────────────────────
models/kokoro/
.cache/
*.onnx
voices.bin

# ── IDE / OS ────────────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*.bak
resume_conversations_*.md
logs/
artifacts/release_soak*.json
.worktrees/

# Cursor delegation worktrees
.jarvis/
